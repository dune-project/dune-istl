from setuptools import setup, find_packages

setup(name="${DUNE_MOD_NAME}",
    namespace_packages=['dune'],
    description="${DUNE_ISTL_DESCRIPTION}",
    version="${DUNE_ISTL_VERSION}",
    author="${DUNE_ISTL_AUTHOR}",
    author_email="${DUNE_ISTL_AUTHOREMAIL}",
    packages = find_packages(),
    zip_safe = 0,
    package_data = {'': ['*.so']}
    install_requires = "${DUNE_ISTL_PYTHON_REQUIRES}".split(' ')
  )
